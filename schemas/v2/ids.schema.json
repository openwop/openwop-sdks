{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openwop.dev/spec/v2/ids.schema.json",
  "title": "OpenWOP v2 identifier grammars (RFC 0170 \u00a7D.1)",
  "description": "One $defs entry per id kind. Every id field in every v2 schema, OpenAPI parameter and response body $refs its kind. `minted` records who mints the id: host (opaque, checkable: `^[A-Za-z0-9._~-]{16,128}$` after any tenant segment), author (a workflow/pack author chooses it; the v1 grammar stands), registry (the pack registry mints it). Tenant-bound kinds (runId, interruptId, subscriptionId, deliveryId, effectId) carry the tenant segment: a host MUST reject an id whose tenant segment is not the caller's.",
  "$comment": "PATH PROJECTION (RFC 0184 \u00a7A.1). A tenant-bound id is TWO segments joined by `/`, and a URL path parameter is ONE segment, so the id must be projected. ENCODE: UTF-8 the id, then per byte emit it verbatim if it is in `[A-Za-z0-9._-]`, else `~` + two UPPERCASE hex digits (`/`\u2192`~2F`, `:`\u2192`~3A`, `~`\u2192`~7E`). DECODE: replace every `~HH`; a `~` not followed by two hex digits is `400 validation_error`. The escape marker is `~` and NOT `%` because `~` is RFC 3986 \u00a72.3 UNRESERVED: an intermediary has no license to rewrite it, so `~2F` reaches the origin byte-for-byte. `%2F` does not \u2014 handling it correctly requires an intermediary to distinguish a percent-encoded RESERVED octet (which \u00a76.2.2.2 says it MUST NOT decode) from an unreserved one (which it SHOULD), and deployed front doors demonstrably do not: a reference host lost every bound id at its origin to exactly this. The codec is TOTAL over bytes, so it cannot be invalidated by a later widening of any id grammar. It is injective because it escapes its own marker \u2014 but ONLY over its own output, which is why \u00a7A.2 forbids MINTING a tenant-bound id containing `~`: an unprojected legacy segment carrying a literal `~` is the one ambiguous input, and a host can resolve those from its OWN store because they are its own ids.APPLY EXACTLY ONCE: the codec is NOT idempotent \u2014 `a~3Ab` encodes to `a~7E3Ab`, which is required for injectivity but means a second application silently corrupts, and nothing marks an encoded id as already encoded. Both reporting hosts found they compose projections on one read path already (one of them caught itself writing two `payload:` keys where the second silently won), so the invariant is stated on the VALUE, not the call site: project where an id LEAVES the host, and never re-encode what was read back from its own output. SCOPE: only the TENANT-BOUND kinds. `nodeId` is author-chosen, carries no tenant segment and already admits `:` by design \u2014 projecting it would change ids that are legal today, which is the cost base64url was rejected for. `Idempotency-Key` (`idempotency.md` \u00a7Layer 1, `^[A-Za-z0-9._~-]{22,128}$`) is caller-supplied and tilde-LEGAL; it is not an id kind, \u00a75 does not bind it, and it is NOT projected \u2014 stated because \"escape `~` in identifiers\" is the sentence a later reader would apply to it. LENGTH: the `{16,128}` bounds govern the ID, never the projected segment. A bound id is already up to 257 characters before any byte escapes, so a validator that checks the projected segment against an id pattern is checking the wrong string.",
  "type": "object",
  "additionalProperties": false,
  "$defs": {
    "opaque": {
      "type": "string",
      "pattern": "^[A-Za-z0-9._~-]{16,128}$",
      "description": "Host-minted opaque token: no `@`, no whitespace, no `/`; 16\u2013128 characters."
    },
    "tenantId": {
      "type": "string",
      "pattern": "^(anon:)?[A-Za-z0-9._~-]{1,128}$",
      "x-openwop-minted": "host",
      "description": "A tenant identifier. The optional `anon:` prefix is the ONLY colon admitted. Its ORIGINAL rationale here cited RFC 0132, and that citation was WRONG: RFC 0132 puts `anon:` on the **principal** and \u00a7C.1 requires the host to resolve a real `tenant` (the surface owner) \u2014 `{tenant: \"acme\", principal: \"anon:sess-3f9c\"}`. The prefix is kept because a REPORTING HOST was observed minting `tenantId: \"anon:<sid>\"` (`conformance/src/scenarios/test-seam-unauthenticated.test.ts`), and that grammar shipped in v2.0.0; removing it is a major-version change, not an erratum. Whether it belongs on a tenant at all is deferred to v3 (RFC 0184 \u00a7A.3). `:` is a legal path character (RFC 3986 pchar), but the claim that it \"stays safe inside the `<tenantId>/<opaque>` run-id form\" was FALSE ON ARRIVAL: this pattern admitted `anon:`, and all five tenant-bound kinds rejected it, so every runId an anon-tenant host minted was schema-invalid. RFC 0184 \u00a7A.3 widened those five to match. On the wire the question is moot \u2014 the \u00a7A.1 projection escapes `:` to `~3A` like any other non-passthrough byte. The alternative \u2014 renaming anonymous tenants at the cut \u2014 was rejected: it is a write to historical identity, which is the one thing the v2 migration does not do."
    },
    "workspaceId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9._~-]{1,128}$",
      "x-openwop-minted": "host"
    },
    "subjectId": {
      "type": "string",
      "pattern": "^[^\\s/]{1,256}$",
      "description": "Issuer-scoped, stable, opaque (RFC 0170 \u00a7A); the issuer's grammar, never PII.",
      "x-openwop-minted": "host"
    },
    "runId": {
      "type": "string",
      "pattern": "^(anon:)?[A-Za-z0-9._~-]{1,128}/[A-Za-z0-9._~-]{16,128}$",
      "description": "Tenant-bound: `<tenantId>/<opaque>`.",
      "x-openwop-minted": "host",
      "x-openwop-tenant-bound": true
    },
    "interruptId": {
      "type": "string",
      "pattern": "^(anon:)?[A-Za-z0-9._~-]{1,128}/[A-Za-z0-9._~-]{16,128}$",
      "x-openwop-minted": "host",
      "x-openwop-tenant-bound": true
    },
    "subscriptionId": {
      "type": "string",
      "pattern": "^(anon:)?[A-Za-z0-9._~-]{1,128}/[A-Za-z0-9._~-]{16,128}$",
      "x-openwop-minted": "host",
      "x-openwop-tenant-bound": true
    },
    "deliveryId": {
      "type": "string",
      "pattern": "^(anon:)?[A-Za-z0-9._~-]{1,128}/[A-Za-z0-9._~-]{16,128}$",
      "x-openwop-minted": "host",
      "x-openwop-tenant-bound": true
    },
    "effectId": {
      "type": "string",
      "pattern": "^(anon:)?[A-Za-z0-9._~-]{1,128}/[A-Za-z0-9._~-]{16,128}$",
      "x-openwop-minted": "host",
      "x-openwop-tenant-bound": true
    },
    "eventId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9._~-]{16,128}$",
      "x-openwop-minted": "host"
    },
    "traceId": {
      "type": "string",
      "pattern": "^[0-9a-f]{32}$",
      "description": "W3C trace-context trace-id.",
      "x-openwop-minted": "host"
    },
    "spanId": {
      "type": "string",
      "pattern": "^[0-9a-f]{16}$",
      "description": "W3C trace-context parent-id.",
      "x-openwop-minted": "host"
    },
    "keyId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9._~-]{1,128}$",
      "description": "A signing-key identifier (registry `signingKeys[].keyId`, resume-token `kid`, bundle signature `keyId`).",
      "x-openwop-minted": "registry"
    },
    "nodeId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9._~:-]{1,128}$",
      "description": "Author-chosen within a workflow definition.",
      "x-openwop-minted": "author"
    },
    "workflowId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9._~:-]{1,128}$",
      "x-openwop-minted": "author"
    },
    "agentId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9._~:-]{1,128}$",
      "x-openwop-minted": "author"
    },
    "chainId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9._~:-]{1,128}$",
      "x-openwop-minted": "author"
    },
    "pluginId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9._~:-]{1,128}$",
      "x-openwop-minted": "author"
    },
    "templateId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9._~:-]{1,128}$",
      "x-openwop-minted": "author"
    },
    "libraryId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9._~:-]{1,128}$",
      "x-openwop-minted": "author"
    },
    "typeId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_-]*(\\.[a-z][a-zA-Z0-9_-]*)+$",
      "description": "A node type id (`core.ai.callPrompt`); the RFC 0107 grammar. Underscore is admitted because node-pack-manifest.schema.json's `name` pattern admits it and a pack's node type ids are DERIVED from its name \u2014 a pack legally named `vendor.acme.my_tools` must be able to declare `vendor.acme.my_tools.echo`. Without the underscore this kind rejected type ids that a legal pack name generates, which is a constraint that cannot express a legitimate value. maxLength is present because this was the ONLY kind whose pattern left length unbounded \u2014 every other $def bounds it inside the pattern ({1,128}, {16,128}). 256 is taken from the bound node-pack-manifest already carried locally, so this relocates it rather than inventing one, and binding those properties to this kind no longer drops it.",
      "x-openwop-minted": "author",
      "maxLength": 256
    }
  }
}
